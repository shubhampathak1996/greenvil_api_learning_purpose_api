it('returns a 400 on missing required fields', async () => {
 
});

it('returns a 400 If both the fields are not matching', async () => {
 
});
it('returns a 200 on successfully Changing the password', async () => {
 
});
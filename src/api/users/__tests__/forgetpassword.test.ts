it('returns a 400 on missing required fields', async () => {
 
});

it('returns a 400 on invalid email', async () => {
 
});

it('returns a 400 on if the email is not registered', async () => {
 
});

it('returns a 200 on successfully sending email for recovery', async () => {
 
});